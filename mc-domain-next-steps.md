# mc.xactiq.net Next Steps

## Immediate actions

1. expose Mission Control as a stable deployed route
2. attach the custom domain `mc.xactiq.net`
3. verify that the page loads independently and does not rely on Memory Hub navigation
4. use that URL in the Miro system map

## Board usage

Once live, the Miro Platform/System Map should use:
- `https://memory.xactiq.net`
- `https://mc.xactiq.net`

as the two primary product destinations.

## Product meaning

This makes the story much clearer:
- Memory Hub = memory
- Mission Control = operations
- shared canonical layer = backbone
